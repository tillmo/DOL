--- OMG_OntoIOp_current-diff-all.tex
+++ OMG_OntoIOp_current-diff-all.tex
@@ -2544,9 +2544,11 @@
 expressions for some meta class in the
 \termref{abstract syntax} to a \termref{semantic domain}.}
 
-\termdefinition{global environment}{mapping from identifiers (IRIs)
-to values in \termref{semantics domains} representing the
-global knowledge about OMS.}
+\termdefinition{global environment}{}mapping from identifiers (IRIs)
+  to values in \termref{semantics domains} representing representing
+  \DIFdel{the global knowledge about OMS}
+  \DIFadd{semantic information about a set of \termref{documents} (the latter typically being distributed over the internet).}
+ 
 
 \end{definitions}
 
